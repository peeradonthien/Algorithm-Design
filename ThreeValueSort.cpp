#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<int> v(n);
    vector<int> count(4);
    for(int i=0;i<n;i++){
        cin >> v[i];
        count[v[i]]++;
    }
    for(int i=1;i<=3;i++){
        for(int j=i-1;j<)
    }
}